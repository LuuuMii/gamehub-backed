<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cmc.mapper.UserCollectionRecordMapper">

    <select id="selectCountDistinctUser"  resultType="java.lang.Integer">
        SELECT COUNT(DISTINCT user_id) as collect_count
        FROM user_collection_record
        WHERE target_id = #{targetId}
          AND target_type = #{targetType}
          AND is_deleted = '0'
    </select>

</mapper>
